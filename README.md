Instala uv

Si o si. Activa venv. SUPER IMPORTANTE.
```
uv venv
<activa el venv>
```

corre uv task main.py para ejecutar el codigo.

Este proyecto descarga los datasets en tu compu, espera como 1gb solo para eso. Tambien crea un custom dataset asi que espera otro gb mas.
